# This is an example environment file for the Grafana Mobile Alerts project.
# Rename this file to `.env` and adjust the values as needed.

# Database credentials
DB_NAME=sensor_data
DB_USER=sensor_user
DB_PASSWORD=sensor_password

# Fetcher configuration
API_URL=https://www.data199.com/api/pv1/device/lastmeasurement
SENSOR_IDS=DEADBEEF1234,C0FFEE56789A
FETCH_INTERVAL_SECONDS=300

# Grafana admin credentials and locale
GF_SECURITY_ADMIN_USER=admin
GF_SECURITY_ADMIN_PASSWORD=admin

# Traefik/Grafana domain
# Set the domain used by the Traefik rule for Grafana (not required when using direct ports)
GRAFANA_DOMAIN=sensors.example.com

# Project name used in Traefik labels (should be unique per Docker host):
PROJECT_NAME=masensors1
